/**
 * Created by g00dv1n on 01.03.17.
 */
import calendarTypes from './calendar-types'

const state = {
  locale: 'ru',
  LOCALES: ['ru', 'en'],
  currentType: 'default',
  calendarTypes
}

export default state
